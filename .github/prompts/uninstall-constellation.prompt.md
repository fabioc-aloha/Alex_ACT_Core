---
description: "Routes legacy Core uninstall requests to Manager's canonical detect-generate-guide workflow. Use when an older caller invokes Core's compatibility command."
lastReviewed: 2026-08-05
---

# /uninstall-constellation

This is a compatibility command. Manager owns safe constellation removal.
Do not invoke the generic skill tool or reproduce the workflow in Core.

Steps:

1. Verify `alex-act-manager@alex-mall` is installed with `copilot plugin list`.
2. If Manager is absent, provide `copilot plugin install alex-act-manager@alex-mall` and stop until it is loaded.
3. Invoke `/alex-act-manager uninstall-constellation`.
4. Preserve Manager's preview, explicit consent, Windows host-lock boundary, and exact receipt-owned removal.

Remove this compatibility prompt after **2026-11-05** if supported callers no
longer use the Core namespace.
